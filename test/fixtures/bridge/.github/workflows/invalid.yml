name: "invalid"

on:
    push:

jobs:
    build:
        runs-on: "ubuntu-latest"
        steps:
            - uses: "actions/checkout@v4"
              with:
                  definitely-not-real: true
